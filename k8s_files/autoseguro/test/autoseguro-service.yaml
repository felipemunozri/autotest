apiVersion: v1
kind: Service
metadata:
  name: autoseguro-service
  namespace: test-namesapce
spec:
  selector:
    app: autoseguro
  type: ClusterIP
  ports:
    - name: autoseguro
      port: 80
      targetPort: 80
      protocol: TCP
      #nodePort: